# 冷链物流中转仓临时置物架货位编码配对管理系统

## 项目信息

- **项目名称**: 冷链物流中转仓临时置物架货位编码配对管理系统
- **项目路径**: /Users/Admin/Desktop/solo-0601/qyx-0701/qyx-组1/qyx-205
- **前端访问地址**: http://localhost:8125
- **后端API地址**: http://localhost:8135/api/shelf

## 端口配置

| 服务 | 端口 | 绑定地址 |
|------|------|----------|
| 前端 | 8125 | 127.0.0.1 |
| 后端 | 8135 | 127.0.0.1 |
| MySQL | 3351 | 127.0.0.1 |
| Redis | 6424 | 127.0.0.1 |

## 启动命令

```bash
cd /Users/Admin/Desktop/solo-0601/qyx-0701/qyx-组1/qyx-205
docker compose up -d --build
```

## 验证结果

### 后端单独编译
- **命令**: `mvn compile -q`
- **结果**: ✅ 通过

### 前端单独构建
- **命令**: `npm install && npm run build`
- **结果**: ✅ 通过

### Docker整体构建启动
- **命令**: `docker compose up -d --build`
- **结果**: ✅ 通过
- **容器状态**: 4个容器全部正常运行

### localhost与127.0.0.1验证
- **localhost:8125**: ✅ 返回项目页面
- **127.0.0.1:8125**: ✅ 返回同一项目页面
- **一致性**: ✅ 完全一致

### 浏览器页面验证
- **前端页面**: ✅ 可访问 http://localhost:8125
- **后端API**: ✅ 可访问 http://localhost:8135/api/shelf
- **中文显示**: ✅ 标题"冷链物流中转仓 - 置物架货位编码配对管理系统"正常显示

### 核心功能验证
- ✅ 货架建档
- ✅ 编码绑定
- ✅ 编码解绑
- ✅ 编码重分配
- ✅ 反向检索
- ✅ 变更记录
- ✅ 导出对照表

## 技术栈

- **前端**: Vue 3 + Vite + Element Plus + TypeScript
- **后端**: Spring Boot 3.3 + JDK 17 + Maven + JPA
- **数据库**: MySQL 8.0
- **缓存**: Redis 7
- **容器化**: Docker + Docker Compose

## 项目结构

```
qyx-205/
├── backend/                    # Spring Boot 后端
│   ├── src/main/java/com/coldchain/
│   │   ├── controller/         # REST API控制层
│   │   ├── service/impl/       # 业务逻辑层
│   │   ├── repository/         # 数据访问层
│   │   ├── entity/             # 实体类
│   │   ├── dto/                # 请求/响应DTO
│   │   └── config/             # 配置类
│   ├── Dockerfile              # 后端Docker镜像（分层缓存）
│   ├── pom.xml                 # Maven配置
│   └── settings.xml            # Maven镜像配置（阿里云）
├── frontend/                   # Vue3 前端
│   ├── src/                    # 源代码
│   │   ├── api/                # API接口
│   │   └── components/         # 组件
│   ├── Dockerfile              # 前端Docker镜像（分层缓存）
│   ├── nginx.conf              # Nginx配置
│   └── package.json            # 依赖配置
├── docker-compose.yml          # 容器编排
├── .env                        # 环境变量配置
├── start.sh                    # 启动脚本
├── .gitignore                  # Git忽略配置
├── README.md                   # 项目说明
└── qyx-205.md                  # 项目交付文档
```

## Docker分层缓存策略

### 后端Dockerfile
1. **依赖层**: COPY pom.xml + settings.xml → RUN mvn dependency:go-offline
2. **代码层**: COPY src → RUN mvn package
3. **运行层**: COPY JAR → java -jar

### 前端Dockerfile
1. **依赖层**: COPY package.json + package-lock.json → RUN npm ci
2. **代码层**: COPY . → RUN npm run build
3. **运行层**: COPY dist → nginx

## 国内镜像配置

- **Maven**: settings.xml 配置阿里云镜像
- **npm**: Dockerfile 中使用腾讯云镜像（https://mirrors.cloud.tencent.com/npm/）

## 中文乱码修复

- ✅ 数据库连接串 characterEncoding=UTF-8
- ✅ SQL脚本 DEFAULT CHARSET=utf8mb4
- ✅ 前端index.html charset="UTF-8"
- ✅ 后端WebMvcConfig配置UTF-8消息转换器
- ✅ 验证结果：中文正常显示
